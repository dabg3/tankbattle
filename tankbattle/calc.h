float inclination_ratio(int degree);
int inclination_degrees90(int degree);

enum quadrant_n {
        TOP_LEFT = 1,
        TOP_RIGHT,
        BOTTOM_LEFT,
        BOTTOM_RIGHT,
};

int quadrant(int degree);
long mod(long number, long denom);
